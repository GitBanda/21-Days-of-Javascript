// this Map method will map a change across the array
let numbers = [30, 64, 90, 54];

numbers = numbers.map(number => {
  return number / 2;
});

console.log(numbers);  // therefore it Will divide each of the numbers by 2
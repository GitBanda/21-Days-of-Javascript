// The pop method removes the last element at the end of the array
let numbers = ['1','2','3','4','5','6'];
console.log(numbers);      // Prints out the list before the removal  of the last element.
numbers.pop();
console.log(numbers);    // therefore it will execute the data without the last number {number 6}
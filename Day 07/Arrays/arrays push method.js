// The push method adds an element at the end of the array
let numbers = ['1','2','3','4'];
console.log(numbers);      //this will Print out the list before the addition of the element.
numbers.push('5');
console.log(numbers);    // and finally this Will print the list of numbers with the '5' added to the end
// The shift method removes the first element at the beginning of the array
let vowels = ['b','a','e','i','o','u'];
console.log(vowels);      // Prints out the list before the removal  of the first element.
vowels.shift();
console.log(vowels);    // Will print the list of vowels with b removed from the beginning
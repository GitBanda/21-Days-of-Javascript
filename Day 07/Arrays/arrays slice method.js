// The slice method creates a new array sliced from the original array
let vowels = ['a','e','i','o','u','p'];
console.log(vowels);      // Prints out the list before the slicing of the orignal array
console.log(vowels.slice(0,5));    // Will print the list of vowels with the array now sliced to feature only the elements indexed from 0 to 5
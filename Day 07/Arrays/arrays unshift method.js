// The unshift method adds an element at the start of the array
let vowels = ['e','i','o','u'];
console.log(vowels);      // Prints out the list before the addition of the new element.
vowels.unshift('a');
console.log(vowels);    // Will print the list of vowels with the new element now added